{
  "installCommand": "cd ../.. && node -e \"const fs=require('fs');const p=JSON.parse(fs.readFileSync('package.json','utf8'));p.workspaces=['apps/api','packages/*'];fs.writeFileSync('package.json',JSON.stringify(p,null,2));\" && npm install && npm run build --workspace=@kebab-biteri/types",
  "buildCommand": "echo skip",
  "framework": null,
  "rewrites": [
    { "source": "/health", "destination": "/api/health" },
    { "source": "/(.*)", "destination": "/api" }
  ],
  "functions": {
    "api/index.ts": { "maxDuration": 30 },
    "api/health.ts": { "maxDuration": 10 }
  }
}
